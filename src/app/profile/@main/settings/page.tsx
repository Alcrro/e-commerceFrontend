import Settings from '@/components/profile/main/settings/Settings';
import React from 'react';

const page = () => {
  return (
    <div>
      Profile Settings <Settings />
    </div>
  );
};

export default page;
